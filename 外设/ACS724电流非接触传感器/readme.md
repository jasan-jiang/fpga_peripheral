# ACS724 非接触式电流传感器

ACS724 是模拟输出传感器，本身没有 SPI/I²C 数字接口，因此在两个来源工程中没有可直接复制的芯片驱动 `.v` 文件。目录中的 PDF 是硬件规格书。

使用时将 ACS724 `VIOUT` 接到外部 ADC 的模拟输入，再使用本仓库 ADC128S102、AD7984 或板载 ADC 的顶层。电流换算一般为 `I=(VIOUT-VZERO)/Sensitivity`；`VZERO`、灵敏度、量程和滤波电容必须按具体后缀和实测零点设置，不应在通用 RTL 中写死。
